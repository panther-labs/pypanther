query asyncBulkUploadDetectionsStatus($input: jobId!) {
    bulkUploadDetectionsStatus(jobId: $input) {
        message
        status
        results {
            deletedRuleIds
            modifiedRuleIds
            newRuleIds
            totalRuleIds
        }
    }
}